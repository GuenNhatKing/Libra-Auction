# TODO: Hoàn thành database ở đây
databaseChangeLog:
  - changeSet:
      id: 001
      author: guennhatking
      labels: example-label
      context: example-context
      comment: example-comment
      changes:
        - createTable:
            tableName: testTable
            columns:
              - column:
                  name: id
                  type: int
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false